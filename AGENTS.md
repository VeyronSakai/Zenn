# Zenn 記事作成エージェント

Zenn 向け技術記事を作成するためのエージェントワークフロー。

## ワークフロー

```text
[入力: メモ/URL/ドキュメント]
       ↓
  📋 Planner → アウトライン作成
       ↓
  ✍️ Writer → ドラフト執筆
       ↓
  🔍 Reviewer → 品質レビュー
       ↓
  🏷️ Optimizer → メタデータ最適化
       ↓
  🚀 Publisher → Draft PR 作成
       ↓
[出力: Draft PR]
```

## エージェント一覧

| エージェント | 役割 | 成果物 |
| ------------ | ------ | -------- |
| `zenn.planner` 📋 | 構成設計 | `drafts/<slug>/outline.md` |
| `zenn.writer` ✍️ | 本文執筆 | `drafts/<slug>/draft.md` |
| `zenn.reviewer` 🔍 | 品質レビュー | `drafts/<slug>/review.md` |
| `zenn.optimizer` 🏷️ | SEO/メタデータ | `articles/<slug>.md` |
| `zenn.publisher` 🚀 | Draft PR 作成 | GitHub Draft PR |

## ディレクトリ構造

```text
articles/           # 最終出力（Zenn で公開）
drafts/             # 作業用ディレクトリ
  <slug>/
    outline.md      # アウトライン
    draft.md        # ドラフト
    review.md       # レビュー結果
docs/
  zenn-style-guide.md  # スタイルガイド
images/
  <slug>/           # 記事用画像
```

## 使い方

1. Chat で `@zenn.planner` を呼び出し、書きたいテーマを入力
2. 各エージェントのハンドオフボタンでワークフローを進行
3. 最終的に `articles/<slug>.md` が生成される
4. `@zenn.publisher` で Draft PR を自動作成
5. `npx zenn preview` でプレビュー確認
6. `published: true` に変更して PR をマージで公開

## 参考

- [docs/zenn-style-guide.md](docs/zenn-style-guide.md): 文体・フォーマット規約
